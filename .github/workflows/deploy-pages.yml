# نشر نسخة ثابتة من الموقع على GitHub Pages
# النسخة الثابتة بتشتغل بالبيانات الثابتة (fallback) لأن Pages مالهاش قاعدة بيانات أو API routes
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      # مهم: مسار النشر (NEXT_PUBLIC_BASE_PATH) لازم يطابق اسم المستودع
      # (SaddamBarber) وإلا ملفات CSS/JS/الصور كلها ترجع 404 والصفحة تظهر بدون تصميم
      - name: Build static export
        run: |
          # API routes ولوحة التحكم محتاجين سيرفر + قاعدة بيانات → مش جزء من النسخة الثابتة
          rm -rf src/app/api src/app/admin
          npx next build
        env:
          STATIC_EXPORT: "1"
          NEXT_PUBLIC_BASE_PATH: /SaddamBarber

      - uses: actions/configure-pages@v5
        with:
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
